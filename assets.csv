{% extends "base.html" %}
{% block content %}
<div class="banner">Scenario actif: <strong>{{ scenario.name }}</strong> — {{ scenario.description }}</div>

<div class="cards">
  <div class="card"><h3>Events</h3><div class="big">{{ dashboard.summary.total_events }}</div></div>
  <div class="card"><h3>Alerts</h3><div class="big">{{ dashboard.summary.total_alerts }}</div></div>
  <div class="card"><h3>Assets</h3><div class="big">{{ dashboard.summary.total_assets }}</div></div>
  <div class="card"><h3>Parse Success</h3><div class="big">{{ dashboard.summary.parse_success_rate }}%</div></div>
  <div class="card"><h3>Parsed</h3><div class="big">{{ dashboard.summary.parsed }}</div></div>
  <div class="card"><h3>Partial</h3><div class="big">{{ dashboard.summary.partial }}</div></div>
  <div class="card"><h3>Failed</h3><div class="big">{{ dashboard.summary.failed }}</div></div>
</div>

<div class="grid">
  <div class="panel"><h2>Events by Source</h2><canvas id="sourceChart" class="chart"></canvas></div>
  <div class="panel"><h2>Alerts by Severity</h2><canvas id="severityChart" class="chart"></canvas></div>
  <div class="panel"><h2>Top Hosts</h2><canvas id="hostChart" class="chart"></canvas></div>
  <div class="panel"><h2>Top Users</h2><canvas id="userChart" class="chart"></canvas></div>
</div>

<div class="panel">
  <h2>Timeline</h2>
  <canvas id="timelineChart" class="chart"></canvas>
</div>

<div class="panel">
  <h2>Latest Alerts</h2>
  <table>
    <thead><tr><th>ID</th><th>Rule</th><th>Title</th><th>Severity</th><th>Status</th><th>Event</th></tr></thead>
    <tbody>
    {% for a in dashboard.latest_alerts %}
      <tr>
        <td>{{ a.id }}</td>
        <td class="mono">{{ a.rule_id }}</td>
        <td>{{ a.title }}</td>
        <td><span class="sev-{{ a.severity }}">{{ a.severity }}</span></td>
        <td>{{ a.status }}</td>
        <td>{{ a.event_id }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
</div>

<script>
function renderChart(id, type, rows) {
  new Chart(document.getElementById(id), {
    type: type,
    data: { labels: rows.map(x => x.label), datasets: [{ data: rows.map(x => x.value) }] },
    options: { responsive: true, maintainAspectRatio: false, plugins: { legend: { display: false } } }
  });
}
renderChart("sourceChart", "bar", {{ dashboard.by_source | tojson }});
renderChart("severityChart", "bar", {{ dashboard.by_severity | tojson }});
renderChart("hostChart", "bar", {{ dashboard.top_hosts | tojson }});
renderChart("userChart", "bar", {{ dashboard.top_users | tojson }});
renderChart("timelineChart", "line", {{ dashboard.timeline | tojson }});
</script>
{% endblock %}
